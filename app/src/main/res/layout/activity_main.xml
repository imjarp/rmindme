<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context="mx.syca.rmindme.MainActivity"
                android:background="@color/background">


    <Button
            android:id="@+id/btn_start_play"
            android:layout_width="fill_parent"
            android:layout_height="200dp"
            android:layout_above="@+id/center_null_view"
            android:text="@string/start_playing"
            android:layout_margin="28dp"
            android:background="@color/primary"
            android:textColor="@color/white"
            android:textAppearance="@android:style/TextAppearance.Large"
            android:onClick="onClickStartPlaying"
            />



    <View android:id="@+id/center_null_view"
          android:layout_width="0dp"
          android:layout_height="0dp"
          android:layout_centerHorizontal="true"
          android:layout_centerVertical="true"/>

    <Button android:id="@+id/btn_add_items"
            android:layout_width="fill_parent"
            android:layout_height="200dp"
            android:layout_below="@+id/center_null_view"
            android:text="@string/add_items"
            android:layout_margin="28dp"
            android:background="@color/primary"
            android:textColor="@color/white"
            android:textAppearance="@android:style/TextAppearance.Large"
            android:onClick="onClickAddItems"
            />



</RelativeLayout>
